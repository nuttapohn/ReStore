import { Typography } from "@mui/material";

function CheckuotPage() {
    return (
        <Typography>
            Only logger in users should be able to see this
        </Typography>
    );
}

export default CheckuotPage;